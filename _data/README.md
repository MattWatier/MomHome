# `_data`

Seed inputs for MomHome.

## Protected path

`do not edit/` is **read-only for agents**. Matt (or other tools) drop updated CSVs there. Agents may only **read** those files and write derived work into `vault/`.

Do not mutate, overwrite, rename, or delete anything inside `do not edit/`.
